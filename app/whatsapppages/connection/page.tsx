'use client';

import Layout from '@/app/components/Layout';
import WhatsappConnect from '@/app/components/WhatsappConnect';

export default function ConnectionPage() {
  return (
    <Layout>
      <WhatsappConnect />
    </Layout>
  );
}